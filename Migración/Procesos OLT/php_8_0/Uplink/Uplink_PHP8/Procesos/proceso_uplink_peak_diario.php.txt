========================================================================
 Procesos/proceso_uplink_peak_diario.php  ·  Revisión PHP 5.3 -> 8.0
========================================================================
Rol del proceso: por cada OLT calcula el peak máximo del día anterior desde
OLT_TRAFICO_UPLINK_HORA y lo inserta en OLT_TRAFICO_PEAK_DAY (kb y mb).

RESULTADO: SIN CAMBIOS DE CÓDIGO. Ya es compatible con PHP 8.0.

Se revisó y se confirmó:
- Solo SQL + date()/strtotime(). Nada removido en PHP 7/8.
- mysqli + MYSQLI_NUM y "or die(...)" -> OK en PHP 8.0.

Observación (NO corregida): el archivo termina con dos etiquetas de cierre
"?>" seguidas. Es inofensivo y ya estaba así en el original; no afecta a
PHP 8.0. Se dejó igual.

NO se modificó: include '/u01/crontab127/conexion/conexion_db.php'.

Verificación: php -l -> "No syntax errors detected".
========================================================================
