========================================================================
 proceso_uplink_trafico_MA5600T_exped.php  ·  Cambios PHP 5.3 -> 8.0
========================================================================
Rol del proceso: subproceso (hilo) lanzado por nohup desde
proceso_uplink_trafico_MA5600T.php. Recibe por $argv la IP, el server y el
lote; hace telnet a una OLT MA5600T, lee el tráfico de sus puertos uplink y
guarda detalle (OLT_TRAFICO_UPLINK_MA5600T) y peak por hora
(OLT_TRAFICO_UPLINK_HORA). Registra la ejecución hija en MONITOREO_*.

------------------------------------------------------------------------
CAMBIOS APLICADOS (obligatorios: eran errores FATALES en PHP 8.0)
------------------------------------------------------------------------
1) eregi_replace() -> preg_replace()   (2 ocurrencias)
   - Antes:  @eregi_replace("[\n|\r|\n\r]", '', $linea)
   - Ahora:  preg_replace('/[\n|\r|\n\r]/', '', $linea)
   - Clase de caracteres idéntica -> mismo resultado exacto.

2) Definición de constantes bareword (bloque nuevo tras set_charset)
   - USER, PASSWORD, SALTO, SHELL, SHELL_CONFIG, SHELL_CONFIG1, SHELL2,
     SALIR, ESPACIO, ESPACIO2. Se definen con su propio nombre como valor
     solo si no existen. Usadas en los switch de las funciones
     estado_equipo*(). En PHP 8 eran "Undefined constant" (fatal).

------------------------------------------------------------------------
CAMBIOS APLICADOS (limpieza: eran solo Warnings, no fatales)
------------------------------------------------------------------------
3) $b inicializada a 0 en las 4 funciones estado_equipo* (case SHELL).
   Comportamiento idéntico (null == 0); solo evita el Warning en los .log.

------------------------------------------------------------------------
NO SE MODIFICÓ
------------------------------------------------------------------------
- Lógica de telnet ni comandos enviados a la OLT.
- SQL, tablas, columnas, uso de $argv[1..3], ping_ip().
- include '/u01/crontab127/conexion/conexion_db.php' (ruta intacta).
- $ERROR y $ERROR2 ya venían inicializadas en este archivo.

Verificación: php -l -> "No syntax errors detected".
========================================================================
